# Shared workspace coordination

Multiple agents work in this checkout. Before editing, read `coordination/ROSTER.md` for current ownership and `CLAUDE.md` / `docs/AGENT_TASKS.md` for historical context. Obtain a non-overlapping assignment from the integration owner and acknowledge it in your own status. Preserve other agents' changes and commit only explicit owned paths.

Read `coordination/README.md` and any addressed note under `coordination/messages/`. Maintain your own `coordination/status/<agent-id>.md` for acknowledgements, running jobs and handoffs; avoid concurrently rewriting another session's status.

`coordination/ROSTER.md` is the root-maintained ownership authority for simultaneous new arrivals. Send offers/changes to root instead of adding overlapping claims to shared assignment boards. Audit work stays in its assigned research directory; production edits require the exact file delegation listed in the roster. Codex root's task ID is `01a0abd2-e250-7a62-a6d6-91f406c345fd`; the external A2 session `andresarriaga-a8` has separately accepted candidate-feasibility work and is not the C1 integration role.

The additional external session `andresarriaga-8a` keeps its accepted Task S1, DFT saddle search. A Codex internal helper owns Task V1, the local visual workbench in `workbench/`. Core quantum/campaign APIs remain with the Codex integration owner. Source forensics remain with the original scientific helper.

Use actual coordinates and recorded calculation evidence. Numerical convergence does not imply electronic-state identity, chemical accuracy, successful manufacture or a validated molecular machine. Preserve failures, input provenance and original data; expose missing evidence clearly.
